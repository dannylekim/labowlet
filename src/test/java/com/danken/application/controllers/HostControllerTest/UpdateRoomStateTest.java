package com.danken.application.controllers.HostControllerTest;

public class UpdateRoomStateTest {

    //business assumptions

    //can Lock if you have players % 2 and more than 1 team.
    //can start once all the words have been placed (fixme might be moved to updating game state)
    //can Lock if not in play
    //can start if not in play
    //is inPlay once has started
}
